# API Contracts

> Every endpoint, request/response shapes, auth requirements, error codes.

---

<!-- TODO: one entry per endpoint.
Format:

## POST /auth/login
**Auth:** none
**Body:** `{"email": str, "password": str}`
**Returns:** `{"token": str, "user_id": str}`
**Errors:** 400 (validation), 401 (invalid creds), 429 (rate-limited)
**Rate limit:** 10/min
-->
